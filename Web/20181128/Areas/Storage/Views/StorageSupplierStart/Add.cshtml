@{
    ViewBag.Title = "Add";
    Layout = "~/Views/Shared/_Home.cshtml";
}

<form class="layui-form layui-form-pane" action="/Storage/StorageSupplierStart/AddStorageSupplierStart" method="post">
    <div class="layui-card"  id="clearDivinput">
        <div class="layui-card-header">仓储选择申请信息</div>
        <div class="layui-card-body" style="overflow:hidden;">
            <div class="layui-form-item" style="width:15%;">
                <div class="layui-btn" id="btnSupplier" style="width:100%;" title="点击选取仓储选择申请信息">仓储选择申请信息</div>
            </div>
            <div class="layui-form-item" style="width:33%;clear:none !important; float:left;">
                <label class="layui-form-label">申请负责人</label>
                <div class="layui-input-inline">
                    <input id="txtApplyUser" type="text" placeholder="申请负责人" class="layui-input" lay-verify="supplier" disabled>
                    <input id="hdStorageChooseId" type="hidden" name="StorageChooseId" />
                </div>
            </div>
            <div class="layui-form-item" style="width:33%;clear:none !important; float:left;">
                <label class="layui-form-label">申请时间</label>
                <div class="layui-input-inline" >
                    <input id="txtApplyTime" type="text" placeholder="申请时间" class="layui-input" lay-verify="supplier" disabled>
                </div>
            </div>
            <div class="layui-form-item" style="width:33%;clear:none !important; float:left;">
                <label class="layui-form-label">申请内容</label>
                <div class="layui-input-inline">
                    <input id="txtApplyContent" type="text" placeholder="申请内容" class="layui-input" lay-verify="supplier" disabled>
                </div>
            </div>
            <div class="layui-form-item" style="width:33%;clear:none !important; float:left;">
                <label class="layui-form-label">申请备注</label>
                <div class="layui-input-inline">
                    <input id="txtApplyRemark" type="text" placeholder="申请备注" class="layui-input" disabled> 
                </div>
            </div>
            <div class="layui-form-item" style="width:33%;clear:none !important; float:left;">
                <label class="layui-form-label">意向供应商</label>
                <div class="layui-input-inline">
                    <input id="txtAffirmStorageIdName" type="text" placeholder="意向供应商" class="layui-input" lay-verify="supplier" disabled> 
                    <input id="hdSupplierId" type="hidden" name="SupplierId" />
                </div>
            </div>
        </div>
    </div>
    <div class="layui-card">
        <div class="layui-card-header">仓储供应商启用信息</div>
        <div class="layui-card-body" style="overflow:hidden;"> 
            <div class="layui-form-item" style="width:33%;clear:none !important; float:left;">
                <label class="layui-form-label"style="width:130px;"><em>*</em>正式运作时间</label>
                <div class="layui-input-inline"style="width:170px;">
                    <input id="txtOfficialTime" type="text" name="OfficialTime" lay-verify="required" placeholder="正式运作时间" class="layui-input"> 
                </div>
            </div>
            <div class="layui-form-item" style="width:33%;clear:none !important; float:left;">
                <label class="layui-form-label">参与培训</label>
                <div class="layui-input-block">
                    <input type="checkbox" checked lay-skin="switch" lay-filter="isCultivate" lay-text="是|否">
                    <input id="hdIsCultivate" type="hidden" name="IsCultivate" value="1" />
                </div>
            </div>
            <div class="layui-form-item" style="width:33%;clear:none !important; float:left;">
                <label class="layui-form-label">签订合同</label>
                <div class="layui-input-block">
                    <input type="checkbox" checked lay-skin="switch" lay-filter="signContract" lay-text="是|否">
                    <input id="hdSignContract" type="hidden" name="SignContract" value="1" />
                </div>
            </div>
            <div id="divContractBeginTime" class="layui-form-item" style="width:33%;clear:none !important; float:left;">
                <label class="layui-form-label">合同开始时间</label>
                <div class="layui-input-inline">
                    <input id="txtContractBeginTime" type="text" name="ContractBeginTimes"  placeholder="合同开始时间" class="layui-input">
                </div>
            </div>
            <div id="divContractEndTime" class="layui-form-item" style="width:33%;clear:none !important; float:left;">
                <label class="layui-form-label">合同结束时间</label>
                <div class="layui-input-inline">
                    <input id="txtContractEndTime" type="text" name="ContractEndTimes" placeholder="合同结束时间" class="layui-input">
                </div>
            </div>
            <div id="divContractNumber" class="layui-form-item" style="width:33%;clear:none !important; float:left;">
                <label class="layui-form-label">合同编号</label>
                <div class="layui-input-inline">
                    <input id="txtContractNumber" type="text" name="ContractNumber"  placeholder="合同编号" class="layui-input">
                </div>
            </div>
            <div class="layui-form-item" style="width:33%;clear:none !important; float:left;">
                <label class="layui-form-label"><em>*</em>结账周期</label>
                <div class="layui-input-inline">
                    <input id="txtAccountsPeriod" type="text" name="AccountsPeriod" lay-verify="required" placeholder="结账周期" class="layui-input">
                </div>
            </div>
            <div class="layui-form-item" style="width:33%;clear:none !important; float:left;">
                <label class="layui-form-label"><em>*</em>押金金额</label>
                <div class="layui-input-inline">
                    <input id="txtDeposit" type="text" name="Deposit" lay-verify="required" placeholder="押金金额" class="layui-input">
                </div>
            </div>
            <div class="layui-form-item">
                <label class="layui-form-label">供应商编号</label>
                <div class="layui-input-block">
                    <textarea id="txtSCMNumber" placeholder="SCM供应商编号" name="SCMNumber" class="layui-textarea" maxlength="100"></textarea>
                </div>
            </div>
            <div class="layui-form-item">
                <label class="layui-form-label">采购编号</label>
                <div class="layui-input-block">
                    <textarea id="txtProcurementNumber" placeholder="采购系统供应商编号" name="ProcurementNumber" class="layui-textarea" maxlength="100"></textarea>
                </div>
            </div>
            <div class="layui-form-item">
                <label class="layui-form-label">培训简介</label>
                <div class="layui-input-block">
                    <textarea id="txtCultivateIntro" placeholder="培训简介" name="CultivateIntro" class="layui-textarea"></textarea>
                </div>
            </div>
            <div class="layui-form-item">
                <div class="layui-form-item">
                    <label class="layui-form-label">合作事项说明</label>
                    <div class="layui-input-block">
                        <textarea id="txtCooperationRemark" placeholder="合作事项说明" name="CooperationRemark"  maxlength="400" class="layui-textarea"></textarea>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <div class="layui-card">
        <div class="layui-card-header">附件信息</div>
        <div class="layui-card-body" style="overflow:hidden;">
            <div class="layui-form-item">
                <input type="hidden" id="hdfilelist" name="SuppFileListes" />
                <div class="layui-input-inline" style="width:800px;overflow:hidden;">
                    <div class="layui-btn-group" style="float:left;overflow:hidden;">
                        <div class="layui-inline">
                            <div class="layui-btn layui-btn-normal" id="btnUpload">上传文件</div>
                        </div>
                    </div>
                    <script type="text/html" id="temfilelists">
                        {{each list as c}}
                        <div class="layui-btn-group" style="margin-bottom:5px;">
                            <div class="layui-btn layui-btn-primary">
                                <span class="modify" data-id="{{c.id}}">{{c.filename}}</span>{{c.ext}}
                                <em onclick="downfile('{{c.id}}')"><i class="layui-icon layui-icon-download-circle"></i></em>
                                <em onclick="delfilelist('{{c.id}}')"><i class="layui-icon layui-icon-close-fill"></i></em>
                            </div>
                        </div>
                        {{/each}}
                    </script>
                    <script type="text/html" id="temdictlist">
                        {{each list as c}}
                        <option value="{{c.id}}">{{c.name}}</option>
                        {{/each}}
                    </script>
                </div>
                <table class="layui-table">
                    <thead>
                        <tr>
                            <th>文件类型</th>
                            <th>文件地址</th>
                            <th>操作</th>
                        </tr>
                    </thead>
                    <tbody id="filelist"></tbody>
                </table>
                <script type="text/html" id="temfilelist">
                    {{each list as c}}
                    <tr>
                        <td>{{c.filename}}</td>
                        <td>{{c.path}}</td>
                        <td><a class="layui-btn layui-btn-danger layui-btn-sm" onclick="delfilelist('{{c.id}}')">删除</a></td>
                    </tr>
                    {{/each}}
                </script>
            </div>
            <div class="layui-form-item">
                <div class="layui-input-block" style="width:20%;margin:0 auto !important;">
                    <button class="layui-btn" lay-submit="" lay-filter="btnSave">保存</button>
                    <a class="layui-btn" id="btnGoBack">返回</a>
                </div>
            </div>
        </div>
    </div>
</form>
@section scripts{
    <script>
        var filelist = [];
        function initfiles() {
            $("#filelist").html(template('temfilelist', { list: filelist }));
            $("#hdfilelist").val(JSON.stringify(filelist));
        }
        function delfilelist(ids) {
            var tem = [];
            for (var i in filelist) {
                if (filelist[i].id != ids) {
                    tem.push(filelist[i]);
                }
            }

            filelist = tem;
            initfiles();
        }
        function setfilename(ids, names) {
            for (var i in filelist) {
                if (filelist[i].id == ids) {
                    filelist[i].filename = names;
                }
            }
            $("#hdfilelist").val(JSON.stringify(filelist));
        }
        function downfile(ids) {
            for (var i in filelist) {
                if (filelist[i].id == ids) {
                    location.href = '/index/down?url=' + filelist[i].path + "&name=" + filelist[i].filename + "&ext=" + filelist[i].ext;
                }
            }
        }

        layui.use(['form', 'element', 'laydate', 'upload'], function () {
            var form = layui.form, ele = layui.element, laydate = layui.laydate; 

            // 开关
            form.on('switch(signContract)', function (data) {
                $("#hdSignContract").val(this.checked ? "1" : "0");

                if ($("#hdSignContract").val() == 1) {
                    $("#divContractBeginTime").show();
                    $("#divContractEndTime").show();
                    $("#divContractNumber").show();
                }
                else if ($("#hdSignContract").val() == 0) { 
                    $("#divContractBeginTime").hide();
                    $("#divContractEndTime").hide();
                    $("#divContractNumber").hide();
                    $("#txtContractBeginTime").val("")
                    $("#txtContractEndTime").val("")
                    $("#txtContractNumber").val("")
                }
            });
            // 开始日期
            var insStart = laydate.render({
                elem: '#txtContractBeginTime'
                ,type: 'datetime'
               , format: 'yyyy/MM/dd HH:mm:ss'
                ,done: function (value, date) {
                    // 更新结束日期的最小日期
                    insEnd.config.min = lay.extend({}, date, {
                        month: date.month - 1
                    });

                    // 自动弹出结束日期的选择器
                    insEnd.config.elem[0].focus();
                }
            });

            // 结束日期
            var insEnd = laydate.render({
                elem: '#txtContractEndTime'
                , type: 'datetime'
               , format: 'yyyy/MM/dd HH:mm:ss'
                , min: 0
                , btns: ['clear', 'confirm']
                , done: function (value, date) {
                    //更新开始日期的最大日期
                    insStart.config.max = lay.extend({}, date, {
                        month: date.month - 1
                    });
                }
            });

            //验证运作时间
            laydate.render({
                elem: '#txtOfficialTime'
               , type: 'datetime'
               , format: 'yyyy/MM/dd HH:mm:ss'
            });
             
            //// 开关
            //form.on('switch(signContract)', function (data) {
            //    $("#hdSignContract").val(this.checked ? "1" : "0");
            //});
            //form.on('switch(isCultivate)', function (data) {
            //    $("#hdisCultivate").val(this.checked ? "1" : "0");
            //});

            form.verify({
                supplier: function (value, item) {
                    if (!value) {
                        return '供应商信息不能为空！';
                    }
                }
            });

            // 上传文件
            $(document).on('click', '#btnUpload', function () {
                top.window.filedialog = top.layui.layer.open({
                    type: 2,
                    shade: 0.2,
                    title: '文件上传',
                    content: '/supplier/SupplierTransport/files?url=/Storage/StorageSupplierStart/Index&type=start',
                    area: ['450px', '300px'],
                });
            });

            ////文件上传
            //layui.upload.render({
            //    elem: '#btnUpload'
            //   , url: '/index/upload?type=storage'
            //   , accept: 'file'
            //   , auto: true
            //   , done: function (res) {
            //       if (res.flag == "ok") {
            //           filelist.push({ id: new Date().getTime(), path: res.path, filename: res.filename, ext: res.ext });
            //           initfiles();
            //       }
            //       return false;
            //   }
            //}); 

            // 保存
            form.on('submit(btnSave)', function (data) {
                $.ajax({
                    type: "POST",
                    url: "/Storage/StorageSupplierStart/AddStorageSupplierStart",
                    data: data.field,
                    async: false,
                    success: function (data) {
                        if (data.flag == 'success') {
                            layui.layer.msg('添加成功');
                            location.href = '/Storage/StorageSupplierStart/Index';
                        }
                        if (data.flag == "fail") {
                            layui.layer.msg('信息添加失败');
                        }
                    }
                });
                return false;
            });
        });

        //添加新文件(新增)
        function setnewfiles(filename, path) {
            filelist.push({ id: new Date().getTime(), path: path, filename: filename, ext: '' });
            initfiles();
        }

        $(function () {
             
            //点击后退按钮，清除form表单中文本框的值
            $("#clearDivinput input").val("");

            // 返回
            $(document).on('click', '#btnGoBack', function () {
                location.href = '/Storage/StorageSupplierStart/Index';
            });

            // 供应商信息
            $(document).on('click', '#btnSupplier', function () {
                GetSupplier();
            });

            // 修改上传上级名称
            $(document).on('click', '.modify', function () {
                var td = $(this);
                var txt = $.trim(td.text());
                var input = td.data("type") == "area" ? $("<textarea>" + $.trim(td.html().replace(/<br>/g, "\n")) + "</textarea>") : $("<input type='text'value='" + txt + "'/>");
                input.width(td.width() + 10).height(td.height() + 10);
                td.html(input);
                input.click(function () { return false; });
                input.trigger("focus");
                input.blur(function () {
                    var str = input.val().replace(/\n/g, '<br>')
                    if (!str) {
                        layui.layer.msg('文件名称不能为空');
                        td.html(txt);
                        return;
                    }
                    td.html(str);
                    setfilename(td.data("id"), str);
                });
                return false;
            });
        });

        // 设置供应商信息
        function setinfo(id, applyuser, applytime, applycontent, applyremark, affirmstorageidname, hdsupplierid) {
            $("#hdStorageChooseId").val(id);
            $("#txtApplyUser").val(applyuser);
            $("#txtApplyTime").val(applytime);
            $("#txtApplyContent").val(applycontent);
            $("#txtApplyRemark").val(applyremark);
            $("#txtAffirmStorageIdName").val(affirmstorageidname);
            $("#hdSupplierId").val(hdsupplierid);
        }

        // 仓储选择申请信息
        function GetSupplier() {
            top.window.topdialog = top.layui.layer.open({
                type: 2,
                shade: 0.2,
                title: '仓储选择申请信息',
                content: '/Storage/StorageSupplierStart/SupplierStartDetail?url=/Storage/StorageSupplierStart/Index', 
                area: ['1100px', '600px'],
            });
        }
    </script>
}